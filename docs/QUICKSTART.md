# Быстрый старт - Локальный запуск

Пошаговое руководство для первого запуска RealtyBot-Bali локально (без VPS).

## Обзор

Это руководство поможет вам:
1. Зарегистрироваться во всех необходимых сервисах
2. Получить все API ключи
3. Настроить проект
4. Запустить бота локально с PostgreSQL

**Примерное время:** 30-40 минут

---

## Шаг 1: Получение API ключей

### 1.1 Apify (обязательно)

**Назначение:** Сбор объявлений с Facebook Marketplace

**Инструкция:**
1. Перейдите на https://apify.com/
2. Нажмите "Start for free" / "Sign up"
3. Зарегистрируйтесь (можно через Google/GitHub)
4. После регистрации перейдите: **Settings → Integrations**
5. Скопируйте **Personal API token** (начинается с `apify_api_...`)

**Что нужно знать:**
- Free tier дает $5 бесплатных кредитов
- Этого хватит на ~100-200 запусков актора
- Paid plans начинаются от $49/месяц

**Сохраните:** `apify_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

### 1.2 Groq (обязательно)

**Назначение:** Бесплатная LLM-проверка наличия кухни (Уровень 1 фильтрации)

**Инструкция:**
1. Перейдите на https://console.groq.com/
2. Зарегистрируйтесь (можно через Google/GitHub)
3. После регистрации перейдите: **API Keys**
4. Нажмите "Create API Key"
5. Введите название (например: `realty-bot`)
6. Скопируйте ключ (показывается только один раз!)

**Что нужно знать:**
- Полностью бесплатно
- Лимиты: 14,400 запросов в день, 30 запросов в минуту
- Используется модель: `llama-3.1-8b-instant`

**Сохраните:** `gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

### 1.3 Anthropic Claude (обязательно)

**Назначение:** Детальный анализ объявлений и генерация сообщений (Уровень 2 фильтрации)

**Инструкция:**
1. Перейдите на https://console.anthropic.com/
2. Нажмите "Sign up"
3. Зарегистрируйтесь (email + пароль)
4. **ВАЖНО:** Добавьте платежный метод
   - Перейдите: **Settings → Billing**
   - Добавьте карту (Visa/MasterCard)
   - Списаний не будет, пока не потратите бесплатные $5
5. После добавления карты: **Settings → API Keys**
6. Нажмите "Create Key"
7. Скопируйте ключ (показывается только один раз!)

**Что нужно знать:**
- $5 бесплатных кредитов при регистрации
- Модель Claude 3 Haiku: $0.25 за 1M входных токенов
- ~2000-5000 анализов на бесплатные $5
- После расхода: ~$2-3/месяц при почасовых запусках

**Расчет расходов:**
```
30 объявлений/час × 24 часа = 720 объявлений/день
Если 10% доходят до Claude = 72 анализа/день
72 × $0.001 ≈ $0.072/день ≈ $2.16/месяц
```

**Сохраните:** `sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

### 1.4 Telegram Bot (обязательно)

**Назначение:** Отправка уведомлений о релевантных объявлениях

#### Часть А: Создание бота

**Инструкция:**
1. Откройте Telegram на любом устройстве
2. Найдите бота [@BotFather](https://t.me/botfather)
3. Отправьте команду `/start`
4. Отправьте команду `/newbot`
5. Введите **имя** бота (например: `My Realty Bot`)
6. Введите **username** бота (должен заканчиваться на `bot`)
   - Пример: `my_realty_finder_bot`
   - Username должен быть уникальным
7. BotFather пришлет вам **токен**

**Формат токена:** `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

**Сохраните токен!**

#### Часть Б: Получение Chat ID

**Вариант 1 - Личный чат (проще для теста):**

1. Найдите своего бота в Telegram по username
2. Нажмите "Start" или отправьте `/start`
3. Отправьте боту любое сообщение (например: `test`)
4. Откройте в браузере (замените `<YOUR_TOKEN>` на токен бота):
   ```
   https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
   ```
5. Найдите в JSON-ответе:
   ```json
   {
     "message": {
       "chat": {
         "id": 1234567890
       }
     }
   }
   ```
6. Скопируйте число (например: `1234567890`)

**Вариант 2 - Приватная группа (рекомендуется для production):**

1. Создайте приватную группу в Telegram
2. Добавьте своего бота в группу
3. Сделайте бота администратором:
   - Откройте настройки группы
   - Administrators → Add Administrator
   - Выберите вашего бота
   - Дайте права на отправку сообщений
4. Отправьте любое сообщение в группу
5. Откройте в браузере:
   ```
   https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
   ```
6. Найдите в ответе:
   ```json
   {
     "message": {
       "chat": {
         "id": -1001234567890,
         "type": "supergroup"
       }
     }
   }
   ```
7. Скопируйте число **с минусом** (например: `-1001234567890`)

**Сохраните:**
- Bot Token: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
- Chat ID: `1234567890` или `-1001234567890`

---

## Шаг 2: Установка Docker

### macOS

**Вариант 1 - Docker Desktop (рекомендуется):**
1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop
2. Установите .dmg файл
3. Запустите Docker Desktop
4. Дождитесь, пока Docker запустится (иконка в трее)

**Проверка установки:**
```bash
docker --version
# Должно показать: Docker version 24.0.x

docker-compose --version
# Должно показать: Docker Compose version v2.x.x
```

---

### Linux (Ubuntu/Debian)

**Установка:**
```bash
# Скачать и установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавить текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Применить изменения (или перелогиниться)
newgrp docker

# Установить Docker Compose (если не установлен)
sudo apt-get update
sudo apt-get install docker-compose-plugin
```

**Проверка:**
```bash
docker --version
docker-compose --version

# Проверка прав (должно работать без sudo)
docker ps
```

---

### Windows

**Требования:**
- Windows 10/11 64-bit: Pro, Enterprise, или Education
- WSL 2 backend

**Установка:**
1. Установите WSL 2:
   ```powershell
   wsl --install
   ```
2. Перезагрузите компьютер
3. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop
4. Установите Docker Desktop
5. Запустите Docker Desktop

**Проверка:**
```bash
docker --version
docker-compose --version
```

---

## Шаг 3: Настройка проекта

### 3.1 Создание .env файла

```bash
cd /Users/krvtzz/Code/estate

# Скопировать шаблон
cp .env.example .env
```

### 3.2 Заполнение .env

Откройте файл `.env` в любом текстовом редакторе:

```bash
# На macOS/Linux
nano .env

# Или в VS Code
code .env
```

**Вставьте ваши ключи:**

```env
# ============================================
# Database Configuration (для локального запуска оставьте как есть)
# ============================================
DATABASE_URL=postgresql://realty_user:mysecurepassword123@postgres:5432/realty_bot
POSTGRES_PASSWORD=mysecurepassword123

# ============================================
# Apify API (ЗАМЕНИТЕ на ваш ключ)
# ============================================
APIFY_API_KEY=apify_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# Telegram Bot (ЗАМЕНИТЕ на ваши данные)
# ============================================
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=1234567890

# ============================================
# LLM APIs (ЗАМЕНИТЕ на ваши ключи)
# ============================================
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**Важно:**
- Замените все `xxx` на ваши реальные ключи из Шага 1
- Пароль PostgreSQL можно оставить как есть (это для локального использования)
- Сохраните файл

**Проверка:**
```bash
# Убедитесь, что .env не в Git
git status
# .env не должен быть в списке изменений
```

---

### 3.3 Настройка config.json

Откройте `config.json`:

```bash
nano config.json
# или
code config.json
```

#### Обновление URL для Facebook Marketplace

**Как найти правильные URL:**

1. Откройте https://www.facebook.com/marketplace в браузере
2. Выберите категорию "Property Rentals" или "Homes for Rent"
3. В поиске введите интересующий вас регион:
   - "Bali" для всего Бали
   - "Canggu" для конкретного района
   - "Ubud", "Seminyak", и т.д.
4. Скопируйте URL из адресной строки браузера
5. Добавьте URL в `config.json`

**Пример для Бали:**

```json
{
  "apify": {
    "max_listings": 30,
    "marketplace_urls": [
      "https://www.facebook.com/marketplace/110371158968624/propertyrentals?minPrice=0&maxPrice=999999999",
      "https://www.facebook.com/marketplace/category/propertyrentals?query=bali&minPrice=0&maxPrice=999999999"
    ],
    "locations": [
      "Bali",
      "Canggu",
      "Ubud"
    ]
  },
  "filters": {
    "price": {
      "min": 3000000,
      "max": 8000000,
      "currency": "IDR"
    }
  }
}
```

**Для первого теста (любой регион):**

```json
{
  "apify": {
    "max_listings": 10,
    "marketplace_urls": [
      "https://www.facebook.com/marketplace/category/propertyrentals?minPrice=0&maxPrice=999999999"
    ],
    "locations": [
      "Test"
    ]
  }
}
```

**Остальные настройки можно оставить как есть.**

---

## Шаг 4: Первый запуск

### 4.1 Проверка Docker

Убедитесь, что Docker запущен:

```bash
docker ps
```

Должна появиться таблица (может быть пустой):
```
CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES
```

Если ошибка `Cannot connect to the Docker daemon`:
- На macOS/Windows: запустите Docker Desktop
- На Linux: `sudo systemctl start docker`

---

### 4.2 Сборка и запуск проекта

```bash
cd /Users/krvtzz/Code/estate

# Соберите и запустите контейнеры
docker-compose up --build -d
```

**Что происходит:**
1. Скачиваются Docker образы (Python 3.11, PostgreSQL 15)
2. Собирается образ приложения (устанавливаются зависимости)
3. Запускаются 2 контейнера:
   - `realty-bot-db` (PostgreSQL)
   - `realty-bot` (приложение)
4. Автоматически создается база данных и таблица

**Время:** ~3-5 минут при первом запуске

---

### 4.3 Проверка статуса контейнеров

```bash
docker-compose ps
```

**Ожидаемый результат:**

```
NAME            IMAGE               STATUS
realty-bot      estate-bot          Up
realty-bot-db   postgres:15-alpine  Up (healthy)
```

Оба контейнера должны быть в статусе "Up".

**Если контейнер не запустился:**
```bash
# Смотрите логи
docker-compose logs bot
docker-compose logs postgres
```

---

### 4.4 Первый тестовый запуск

Запустите скрипт вручную (без ожидания cron):

```bash
docker-compose exec bot python3 /app/src/main.py
```

**Что вы увидите:**

```
================================================================================
RealtyBot-Bali started
================================================================================
Initializing components...
Starting scraping process...
Scraped 30 raw listings
Processing listing 1/30: fb_123456789
New listing found: fb_123456789
Level 0 PASSED
Level 1 PASSED
Level 2 PASSED
Telegram notification sent successfully
...
================================================================================
Processing complete!
Total listings processed: 30
New listings: 30
Notifications sent: 5
================================================================================
```

**Время выполнения:** ~5-10 минут (зависит от количества объявлений)

---

### 4.5 Просмотр логов в реальном времени

В отдельном терминале:

```bash
# Логи бота
docker-compose logs -f bot

# Все логи (бот + PostgreSQL)
docker-compose logs -f

# Выход из просмотра логов: Ctrl+C
```

---

## Шаг 5: Проверка результатов

### 5.1 Проверка базы данных

```bash
# Подключитесь к PostgreSQL
docker-compose exec postgres psql -U realty_user -d realty_bot
```

**Внутри psql выполните:**

```sql
-- Общее количество объявлений
SELECT COUNT(*) FROM fb_listings;

-- Сколько отправлено в Telegram
SELECT COUNT(*) FROM fb_listings WHERE sent_to_telegram = true;

-- Последние 5 объявлений
SELECT title, price, sent_to_telegram, created_at 
FROM fb_listings 
ORDER BY created_at DESC 
LIMIT 5;

-- Выход из psql
\q
```

**Ожидаемый результат:**
```
 count 
-------
    30
(1 row)
```

---

### 5.2 Проверка Telegram

1. Откройте Telegram
2. Перейдите в чат с ботом (или в группу)
3. Проверьте, пришли ли уведомления

**Формат уведомления:**
```
🏡 Новый вариант!

Выжимка: [Описание от Claude]
Цена: Rp 5,000,000
Телефон: +628123456789
Ссылка: [URL]

---
Сообщение (скопируй и вставь):

(EN):
Hello! I'm interested in...

(ID):
Halo! Saya tertarik...
```

---

### 5.3 Проверка логов приложения

```bash
# Логи на хосте
tail -f logs/realty_bot.log

# Или
cat logs/realty_bot.log
```

---

## Шаг 6: Настройка автоматического запуска (опционально)

По умолчанию бот запускается **каждый час** через cron внутри контейнера.

### Проверка cron

```bash
# Проверьте, что cron работает
docker-compose exec bot ps aux | grep cron

# Должно показать процесс cron
```

### Просмотр расписания

```bash
docker-compose exec bot crontab -l
```

**Вывод:**
```
0 * * * * cd /app && /usr/local/bin/python3 /app/src/main.py >> /var/log/cron.log 2>&1
```

### Изменение расписания

Если хотите изменить частоту запусков, отредактируйте файл `crontab`:

```bash
nano crontab
```

**Примеры:**

```bash
# Каждые 30 минут
*/30 * * * * cd /app && /usr/local/bin/python3 /app/src/main.py >> /var/log/cron.log 2>&1

# Каждые 2 часа
0 */2 * * * cd /app && /usr/local/bin/python3 /app/src/main.py >> /var/log/cron.log 2>&1

# Каждые 6 часов (для экономии Apify кредитов)
0 */6 * * * cd /app && /usr/local/bin/python3 /app/src/main.py >> /var/log/cron.log 2>&1
```

После изменений пересоберите контейнер:

```bash
docker-compose up --build -d
```

---

## Распространенные проблемы

### ❌ Проблема: "Cannot connect to Docker daemon"

**Решение:**
- macOS/Windows: Запустите Docker Desktop
- Linux: `sudo systemctl start docker`

---

### ❌ Проблема: "No listings found" или "Scraped 0 listings"

**Возможные причины:**

1. **Неверный Apify API ключ**
   ```bash
   # Проверьте ключ в .env
   cat .env | grep APIFY
   ```

2. **Неправильные URL в config.json**
   - Убедитесь, что URL валидны
   - Откройте URL в браузере
   - Должны быть видны объявления

3. **Исчерпаны кредиты Apify**
   - Проверьте на https://console.apify.com/
   - Раздел: Usage & Billing

**Отладка:**
```bash
docker-compose logs bot | grep -i apify
```

---

### ❌ Проблема: Telegram не отправляет сообщения

**Проверьте:**

1. **Bot Token правильный:**
   ```bash
   # Проверьте в .env
   cat .env | grep TELEGRAM_BOT_TOKEN
   
   # Протестируйте токен в браузере
   https://api.telegram.org/bot<YOUR_TOKEN>/getMe
   ```

2. **Chat ID правильный:**
   ```bash
   cat .env | grep TELEGRAM_CHAT_ID
   
   # Для группы должен начинаться с минуса: -1001234567890
   # Для личного чата: положительное число
   ```

3. **Бот добавлен в группу (если используете группу)**
   - Бот должен быть администратором

**Отладка:**
```bash
docker-compose logs bot | grep -i telegram
```

---

### ❌ Проблема: Groq/Claude API ошибки

**Ошибка: 401 Unauthorized**

```bash
# Проверьте ключи
cat .env | grep GROQ_API_KEY
cat .env | grep ANTHROPIC_API_KEY
```

**Для Claude: "authentication_error"**
- Убедитесь, что добавили платежный метод на https://console.anthropic.com/

**Для Groq: rate limit**
- Превышен лимит 30 запросов/минуту
- Уменьшите `max_listings` в `config.json`

---

### ❌ Проблема: База данных не создалась

```bash
# Проверьте статус PostgreSQL
docker-compose logs postgres

# Пересоздайте базу
docker-compose down -v
docker-compose up -d
```

---

### ❌ Проблема: "Port already in use"

**Если порт 5432 занят:**

Измените порт в `docker-compose.yml`:

```yaml
services:
  postgres:
    ports:
      - "5433:5432"  # Вместо 5432:5432
```

---

## Полезные команды

### Управление контейнерами

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Перезапуск
docker-compose restart

# Пересборка
docker-compose up --build -d

# Статус
docker-compose ps

# Логи
docker-compose logs -f bot

# Остановка и удаление (данные БД сохранятся)
docker-compose down

# Удаление всего включая данные
docker-compose down -v
```

### Работа с базой данных

```bash
# Подключение к PostgreSQL
docker-compose exec postgres psql -U realty_user -d realty_bot

# Бэкап БД
docker-compose exec postgres pg_dump -U realty_user realty_bot > backup.sql

# Восстановление БД
docker-compose exec -T postgres psql -U realty_user realty_bot < backup.sql
```

### Ручной запуск скрипта

```bash
# Запустить сбор вручную
docker-compose exec bot python3 /app/src/main.py

# Зайти в контейнер
docker-compose exec bot /bin/bash

# Проверить переменные окружения
docker-compose exec bot env | grep API
```

---

## Следующие шаги

После успешного локального запуска:

1. **Настройте фильтры под себя:**
   - Отредактируйте `config.json`
   - Добавьте нужные стоп-слова
   - Настройте ценовой диапазон

2. **Оптимизируйте расписание:**
   - Измените частоту в `crontab`
   - Учитывайте лимиты Apify

3. **Подготовьтесь к деплою на VPS:**
   - См. [REQUIREMENTS.md](REQUIREMENTS.md)
   - Подготовьте VPS
   - Настройте домен (опционально)

---

## Получение помощи

**Если застряли:**

1. Проверьте [TROUBLESHOOTING.md](TROUBLESHOOTING.md) — там 80+ сценариев
2. Посмотрите логи: `docker-compose logs bot`
3. Проверьте `.env` и `config.json`
4. Создайте Issue на GitHub с описанием проблемы

**При создании Issue приложите:**
- Описание проблемы
- Что вы пытались сделать
- Логи: `docker-compose logs bot > logs.txt`
- Версию Docker: `docker --version`

---

## Чеклист готовности

Перед первым запуском убедитесь:

- [ ] Docker установлен и запущен
- [ ] Получены все API ключи:
  - [ ] Apify API Key
  - [ ] Telegram Bot Token
  - [ ] Telegram Chat ID
  - [ ] Groq API Key
  - [ ] Anthropic API Key (с добавленной картой)
- [ ] Создан файл `.env` из `.env.example`
- [ ] Все ключи добавлены в `.env`
- [ ] Обновлены URL в `config.json`
- [ ] `.env` в `.gitignore` (уже добавлен)
- [ ] Контейнеры запущены: `docker-compose ps`
- [ ] Тестовый запуск выполнен успешно
- [ ] Проверена база данных
- [ ] Получено хотя бы одно уведомление в Telegram

---

## Поздравляем! 🎉

Если вы дошли до этого момента и всё работает — вы успешно запустили RealtyBot-Bali!

Бот теперь будет автоматически:
1. Собирать объявления каждый час
2. Фильтровать их через 3 уровня
3. Отправлять релевантные варианты в Telegram
4. Сохранять всё в PostgreSQL для дедупликации

**Приятного использования!**
