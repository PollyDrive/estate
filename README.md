# RealtyBot-Bali

Автоматизированный агент для мониторинга недвижимости на Facebook Marketplace с многоуровневой AI-фильтрацией.

## Описание

RealtyBot-Bali — это бот, который 24/7 отслеживает новые объявления об аренде жилья на Facebook Marketplace в заданных районах Бали. Бот использует трёхуровневую воронку фильтрации для отсева 99% нерелевантных объявлений и отправляет только подходящие варианты в приватную Telegram-группу.

### Особенности

- **Автоматический сбор данных** через Apify Facebook Marketplace Scraper
- **Трёхуровневая фильтрация**:
  - Уровень 0: Жесткие фильтры (цена, стоп-слова, обязательные слова)
  - Уровень 1: Бесплатная LLM-проверка через Groq (Llama 3)
  - Уровень 2: Детальный анализ через Claude 3 Haiku
- **Дедупликация** в PostgreSQL
- **Автоматические уведомления** в Telegram с готовыми текстами на английском и индонезийском
- **Docker-контейнеризация** для простого развертывания
- **Запуск по расписанию** через cron (раз в час)

## Структура проекта

```
estate/
├── src/
│   ├── main.py              # Основной скрипт
│   ├── database.py          # Работа с PostgreSQL
│   ├── filters.py           # Уровень 0: жесткие фильтры
│   ├── llm_filters.py       # Уровни 1 и 2: LLM-фильтры
│   ├── telegram_notifier.py # Отправка уведомлений в Telegram
│   └── apify_scraper.py     # Сбор данных через Apify
├── db/
│   └── init.sql             # SQL-схема базы данных
├── logs/                    # Логи приложения
├── config.json              # Конфигурация (районы, фильтры, стоп-слова)
├── .env                     # Переменные окружения (НЕ коммитить!)
├── .env.example             # Шаблон для .env
├── requirements.txt         # Python-зависимости
├── Dockerfile               # Docker-образ приложения
├── docker-compose.yml       # Оркестрация контейнеров
├── crontab                  # Расписание запуска
└── README.md                # Документация
```

## Установка и развертывание

### Предварительные требования

- Docker и Docker Compose
- Получите API ключи:
  - [Apify](https://apify.com/) - для сбора данных с Facebook
  - [Telegram Bot Token](https://t.me/botfather) - создайте бота
  - [Groq API](https://console.groq.com/) - бесплатный LLM
  - [Anthropic API](https://console.anthropic.com/) - Claude 3 Haiku

### Шаг 1: Клонирование и настройка

```bash
cd /path/to/estate
```

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните `.env` вашими реальными ключами:

```env
DATABASE_URL=postgresql://realty_user:your_secure_password@postgres:5432/realty_bot
POSTGRES_PASSWORD=your_secure_password

APIFY_API_KEY=your_apify_api_key
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id
GROQ_API_KEY=your_groq_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
```

### Шаг 3: Настройка конфигурации

Отредактируйте `config.json` под свои нужды:

```json
{
  "apify": {
    "max_listings": 30,
    "marketplace_urls": [
      "https://www.facebook.com/marketplace/..."
    ]
  },
  "filters": {
    "price": {
      "min": 3000000,
      "max": 8000000
    },
    "stop_words": ["daily", "harian", ...],
    "required_words": ["kitchen", "kitchenette", "dapur"]
  }
}
```

**Важно:** Укажите реальные URL от Facebook Marketplace для ваших районов.

### Шаг 4: Получение Telegram Chat ID

Добавьте бота в приватную группу, затем получите Chat ID:

```bash
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```

Найдите `chat.id` в ответе и добавьте в `.env`.

### Шаг 5: Запуск через Docker

Соберите и запустите контейнеры:

```bash
docker-compose up --build -d
```

Проверьте статус:

```bash
docker-compose ps
docker-compose logs -f bot
```

### Шаг 6: Ручной запуск (опционально)

Для тестирования без ожидания cron:

```bash
docker-compose exec bot python3 /app/src/main.py
```

## Мониторинг

### Просмотр логов

```bash
# Логи бота
docker-compose logs -f bot

# Логи PostgreSQL
docker-compose logs -f postgres

# Логи приложения на хосте
tail -f logs/realty_bot.log
```

### Проверка базы данных

```bash
docker-compose exec postgres psql -U realty_user -d realty_bot

# Внутри psql:
SELECT COUNT(*) FROM fb_listings;
SELECT COUNT(*) FROM fb_listings WHERE sent_to_telegram = true;
```

## Настройка расписания

По умолчанию бот запускается **каждый час**. Чтобы изменить:

1. Отредактируйте файл `crontab`:
```
0 * * * * cd /app && /usr/local/bin/python3 /app/src/main.py >> /var/log/cron.log 2>&1
```

2. Пересоберите контейнер:
```bash
docker-compose up --build -d
```

Примеры расписаний:
- Каждые 30 минут: `*/30 * * * *`
- Каждые 2 часа: `0 */2 * * *`
- Каждый день в 9:00: `0 9 * * *`

## Обновление конфигурации

После изменения `config.json`:

```bash
# Перезапустите контейнер
docker-compose restart bot
```

## Остановка и удаление

```bash
# Остановить
docker-compose stop

# Остановить и удалить контейнеры (данные БД сохранятся)
docker-compose down

# Удалить контейнеры и данные БД
docker-compose down -v
```

## Структура воронки фильтрации

1. **Уровень 0 (Python + Regex)**
   - Проверка ценового диапазона
   - Исключение стоп-слов (daily, harian, посуточно)
   - Обязательное наличие слов (kitchen, kitchenette)
   - Извлечение телефонных номеров

2. **Уровень 1 (Groq - бесплатно)**
   - Подтверждение наличия кухни через Llama 3 8B
   - Простой YES/NO ответ

3. **Уровень 2 (Claude 3 Haiku)**
   - Детальный анализ объявления
   - Генерация выжимки на русском
   - Создание шаблонов сообщений на английском и индонезийском

## Формат Telegram-уведомления

```
🏡 Новый вариант!

Выжимка: [Краткое описание от Claude]
Цена: Rp 5,000,000
Телефон: +628123456789
Ссылка: [URL объявления]

---
Сообщение (скопируй и вставь):

(EN):
Hello! I'm interested in your property listing...

(ID):
Halo! Saya tertarik dengan properti Anda...
```

## Troubleshooting

### Бот не запускается

```bash
# Проверьте логи
docker-compose logs bot

# Проверьте переменные окружения
docker-compose exec bot env | grep API
```

### Нет объявлений

- Проверьте URL в `config.json` — они должны быть валидными
- Убедитесь, что Apify API key активен
- Проверьте лимиты Apify

### Telegram не отправляет

- Убедитесь, что бот добавлен в группу
- Проверьте правильность `TELEGRAM_CHAT_ID`
- Проверьте, что бот является администратором группы

### Ошибки LLM

- Проверьте лимиты API ключей (Groq, Anthropic)
- Убедитесь, что у вас есть кредиты

## Безопасность

- **НЕ коммитьте `.env`** в Git
- Используйте сильные пароли для PostgreSQL
- Храните API ключи в безопасности
- Регулярно обновляйте зависимости

## Лицензия

MIT

## Поддержка

При возникновении проблем:
1. Проверьте логи
2. Убедитесь, что все API ключи валидны
3. Проверьте конфигурацию в `config.json`
