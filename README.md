# RealtyBot-Bali

Автоматизированный агент для мониторинга недвижимости на Facebook Marketplace с многоуровневой AI-фильтрацией.

## Описание

RealtyBot-Bali — это бот, который 24/7 отслеживает новые объявления об аренде жилья на Facebook Marketplace в заданных районах Бали. Бот использует трёхуровневую воронку фильтрации для отсева 99% нерелевантных объявлений и отправляет только подходящие варианты в приватную Telegram-группу.

### Особенности

- **Автоматический сбор данных** через Apify Facebook Marketplace Scraper
- **Трёхуровневая фильтрация**:
  - Уровень 0: Жесткие фильтры (цена, стоп-слова, обязательные слова)
  - Уровень 1: Бесплатная LLM-проверка через Groq (Llama 3)
  - Уровень 2: Детальный анализ через Claude 3 Haiku
- **Дедупликация** в PostgreSQL
- **Автоматические уведомления** в Telegram с готовыми текстами на английском и индонезийском
- **Docker-контейнеризация** для простого развертывания
- **Запуск по расписанию** через cron (раз в час)

## Документация

- **[Требования и настройка](docs/REQUIREMENTS.md)** — подробное руководство по настройке и требованиям
- **[Техническое задание](docs/DESCRIPTION.md)** — оригинальное ТЗ проекта
- **[Решение проблем](docs/TROUBLESHOOTING.md)** — руководство по устранению неполадок

## Быстрый старт

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/YOUR_USERNAME/realty-bot-bali.git
cd realty-bot-bali
```

### Шаг 2: Настройка переменных окружения

```bash
cp .env.example .env
nano .env  # Заполните реальными ключами
```

Необходимые API ключи:
- **Apify API Key** — получите на [apify.com](https://apify.com/)
- **Telegram Bot Token** — создайте бота через [@BotFather](https://t.me/botfather)
- **Telegram Chat ID** — см. [инструкцию](docs/REQUIREMENTS.md#2-telegram-bot)
- **Groq API Key** — зарегистрируйтесь на [console.groq.com](https://console.groq.com/)
- **Anthropic API Key** — получите на [console.anthropic.com](https://console.anthropic.com/)

### Шаг 3: Настройка конфигурации

Отредактируйте `config.json`:

```json
{
  "apify": {
    "marketplace_urls": [
      "https://www.facebook.com/marketplace/YOUR_LOCATION/propertyrentals"
    ]
  },
  "filters": {
    "price": {
      "min": 3000000,
      "max": 8000000
    }
  }
}
```

### Шаг 4: Запуск

```bash
docker-compose up --build -d
```

### Шаг 5: Проверка

```bash
# Статус контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs -f bot

# Ручной запуск для теста
docker-compose exec bot python3 /app/src/main.py
```

## Структура проекта

```
realty-bot-bali/
├── src/                     # Исходный код
│   ├── main.py              # Главный скрипт с логикой воронки
│   ├── database.py          # PostgreSQL интеграция
│   ├── filters.py           # Уровень 0: regex, цены, стоп-слова
│   ├── llm_filters.py       # Уровни 1-2: Groq + Claude
│   ├── telegram_notifier.py # Telegram уведомления
│   └── apify_scraper.py     # Сбор данных с Facebook
├── db/
│   └── init.sql             # SQL-схема базы данных
├── docs/                    # Документация
│   ├── REQUIREMENTS.md      # Требования и настройка
│   ├── DESCRIPTION.md       # Техническое задание
│   └── TROUBLESHOOTING.md   # Решение проблем
├── logs/                    # Логи приложения
├── config.json              # Конфигурация
├── .env.example             # Шаблон credentials
├── requirements.txt         # Python зависимости
├── Dockerfile               # Docker образ
├── docker-compose.yml       # Оркестрация
├── crontab                  # Расписание
└── README.md                # Этот файл
```

## Мониторинг

### Просмотр логов

```bash
# Логи бота
docker-compose logs -f bot

# Логи PostgreSQL
docker-compose logs -f postgres

# Логи приложения на хосте
tail -f logs/realty_bot.log
```

### Проверка базы данных

```bash
docker-compose exec postgres psql -U realty_user -d realty_bot

# Внутри psql:
SELECT COUNT(*) FROM fb_listings;
SELECT COUNT(*) FROM fb_listings WHERE sent_to_telegram = true;
```

## Настройка расписания

По умолчанию бот запускается **каждый час**. Чтобы изменить, отредактируйте `crontab`:

```bash
# Каждый час (по умолчанию)
0 * * * * cd /app && /usr/local/bin/python3 /app/src/main.py >> /var/log/cron.log 2>&1

# Каждые 30 минут
*/30 * * * * cd /app && /usr/local/bin/python3 /app/src/main.py >> /var/log/cron.log 2>&1

# Каждые 2 часа
0 */2 * * * cd /app && /usr/local/bin/python3 /app/src/main.py >> /var/log/cron.log 2>&1
```

После изменений:
```bash
docker-compose up --build -d
```

## Структура воронки фильтрации

**Уровень 0 (Python + Regex)**
- Проверка ценового диапазона
- Исключение стоп-слов (daily, harian, посуточно)
- Обязательное наличие слов (kitchen, kitchenette)
- Извлечение телефонных номеров

**Уровень 1 (Groq - бесплатно)**
- Подтверждение наличия кухни через Llama 3 8B
- Простой YES/NO ответ

**Уровень 2 (Claude 3 Haiku)**
- Детальный анализ объявления
- Генерация выжимки на русском
- Создание шаблонов сообщений на английском и индонезийском

## Формат Telegram-уведомления

```
🏡 Новый вариант!

Выжимка: [Краткое описание от Claude]
Цена: Rp 5,000,000
Телефон: +628123456789
Ссылка: [URL объявления]

---
Сообщение (скопируй и вставь):

(EN):
Hello! I'm interested in your property listing...

(ID):
Halo! Saya tertarik dengan properti Anda...
```

## Управление контейнерами

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Перезапуск
docker-compose restart bot

# Просмотр статуса
docker-compose ps

# Остановка и удаление (данные БД сохранятся)
docker-compose down

# Удаление всего включая данные
docker-compose down -v
```

## Обновление

```bash
# Получить обновления
git pull origin main

# Пересобрать и запустить
docker-compose up --build -d
```

## Безопасность

- **НЕ коммитьте `.env`** в Git (уже в `.gitignore`)
- Используйте сильные пароли для PostgreSQL
- Храните API ключи в безопасности
- Регулярно обновляйте зависимости
- На VPS: установите `chmod 600 .env`

## Требования

**Минимальные:**
- Docker 20.10+
- Docker Compose 2.0+
- 1GB RAM
- 10GB диск

**Рекомендуемые:**
- 2GB+ RAM
- 20GB SSD
- 2+ CPU cores

Подробнее см. [docs/REQUIREMENTS.md](docs/REQUIREMENTS.md)

## Решение проблем

Если возникли проблемы, см. [docs/TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md)

Частые вопросы:
- Бот не запускается → проверьте логи: `docker-compose logs bot`
- Нет объявлений → проверьте URL в `config.json`
- Telegram не отправляет → проверьте Chat ID
- Ошибки LLM → проверьте API ключи и лимиты

## Лицензия

MIT

## Поддержка

При возникновении проблем:
1. Проверьте [TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md)
2. Убедитесь, что все требования выполнены
3. Проверьте логи: `docker-compose logs bot`
4. Создайте Issue на GitHub с подробным описанием

## Contributing

Pull requests приветствуются! Для больших изменений сначала откройте Issue для обсуждения.
